[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: fast unit tests (no network, no DB)
    integration: tests that hit the live cloud bridge
    live_mt5: tests that require a live MT5 connection
